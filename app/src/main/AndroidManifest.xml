<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.santalu.myapplication">

  <uses-permission android:name="android.permission.INTERNET"/>

  <application
      android:allowBackup="true"
      android:icon="@mipmap/ic_launcher"
      android:label="@string/app_name"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:supportsRtl="true"
      android:theme="@style/AppTheme">

    <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:screenOrientation="portrait">
      <intent-filter>
        <action android:name="android.intent.action.MAIN"/>

        <category android:name="android.intent.category.LAUNCHER"/>
      </intent-filter>
    </activity>

    <activity
        android:name=".GridLayoutSampleActivity"
        android:label="@string/sample_grid"
        android:screenOrientation="portrait"/>

    <activity
        android:name=".RecyclerViewSampleActivity"
        android:label="@string/sample_list"
        android:screenOrientation="portrait"/>

    <activity
        android:name=".RecyclerViewComplexSampleActivity"
        android:label="@string/sample_list_complex"
        android:screenOrientation="portrait"/>

    <activity
        android:name=".CardViewSampleActivity"
        android:label="@string/sample_card"
        android:screenOrientation="portrait"/>

    <activity
        android:name=".CollapsingToolbarSampleActivity"
        android:label="@string/sample_collapsing"
        android:screenOrientation="portrait"/>

  </application>

</manifest>